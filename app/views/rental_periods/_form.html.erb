<%= form_with(model: rental_period) do |form| %>
  <% if rental_period.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(rental_period.errors.count, "error") %> prohibited this rental_period from being saved:</h2>

      <ul>
        <% rental_period.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <%= form.hidden_field :item_id %> 
<br>
  <div class="field mb-3">
    <% form.label :from %>
    <% form.date_select :from %>
  </div>

  <div class="field mb-3">
    <%= form.label "How long do you want to rent this item ?" %>
    <br>
    <%= form.date_select :to %>
  </div>

  
<div class="field mb-3" style="visibility: hidden;">
  <% form.label :user_from %>
      <div class="col-sm-10">
        <%= form.select :user_from_id , User.pluck(:name , :id) %>
      </div>
</div>

  <div class="actions">
    <%= form.submit "Rent Me!"%>
  </div>
<% end %>






